{
  "name": "pocketzot",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:links": "node scripts/check-html-links.mjs",
    "build": "node scripts/check-html-links.mjs && tsc && vitest run && vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "happy-dom": "^20.9.0",
    "typescript": "^5.4.5",
    "vite": "^7.0.0",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
