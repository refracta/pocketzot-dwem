{
  "name": "pocketzot",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:links": "node scripts/check-html-links.mjs",
    "build": "node scripts/check-html-links.mjs && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^7.0.0"
  }
}
